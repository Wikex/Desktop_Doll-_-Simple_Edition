# 开发者指南：如何为桌面人偶增加新功能（子球）

如果你想为“桌面人偶”增加一个新的悬浮子功能（例如：翻译、天气、计算器等），请按照以下步骤操作。

---

## 1. 核心逻辑层 (`core/`)
如果你的功能涉及后台逻辑、数据处理或外部 API 调用，请在 `core/` 目录下创建一个新的模块。
- **示例**: `core/weather.py`
- **职责**: 负责获取天气数据，不直接操作 UI，通过 `Signal` (Qt 信号) 将结果传回 UI。

## 2. 用户界面层 (`ui/`)
创建一个新的面板类，通常继承自 `QWidget`，并设置无边框、置顶等属性。
- **参考**: `ui/search_box.py` 或 `ui/notebook.py`
- **关键属性**:
  ```python
  self.setWindowFlags(Qt.FramelessWindowHint | Qt.Tool | Qt.WindowStaysOnTopHint)
  self.setAttribute(Qt.WA_TranslucentBackground)
  ```
- **核心方法**: 需实现 `update_position(ball_x, ball_y)` 以确保面板跟随子球移动。

## 3. 配置与设置 (`utils/` & `ui/settings.py`)
为了让新功能可配置，你需要：
1. **`utils/config.py`**:
   - 在 `DEFAULT_OPTIONS` 中增加 `enable_xxx_ball: True`。
   - 在 `DEFAULT_HOTKEYS` 中增加 `xxx: "alt+x"`。
2. **`ui/settings.py`**:
   - 在 `SettingsDialog.init_ui` 的 `Tab 4: Features` 中增加对应的 `QCheckBox`。
   - 在 `Tab 3: Hotkeys` 中增加对应的 `HotkeyRow`。

## 4. 主程序集成 (`main.py`) - **最关键的一步**
你需要将新功能“粘合”到 `FloatingAssistant` 类中。

### A. 导入模块
```python
from ui.xxx_panel import XXXPanel
from core.xxx import XXXManager
```

### B. 初始化子球 (在 `__init__` 中)
```python
self.xxx_ball = SubBall(
    self.ball,
    text="图标", 
    radius=55,
    angle=math.pi * 0.4, # 设置一个不冲突的角度
    tooltip="功能描述",
    bg_color=QColor(R, G, B, 230)
)
self.sub_balls.append(self.xxx_ball)
```

### C. 注册兄弟节点 (防重叠逻辑)
必须将新球加入到其他所有球的 `siblings` 列表中，并将其他所有球加入新球的 `siblings` 列表。
```python
# 在 main.py 的 siblings 初始化部分
all_balls = [self.clipboard_ball, self.screenshot_ball, self.notebook_ball, 
             self.smart_screenshot_ball, self.record_ball, self.search_ball, self.xxx_ball]
for b in all_balls:
    b.siblings = [other for other in all_balls if other is not b]
```

### D. 建立信号连接
```python
self.xxx_ball.clicked.connect(self.on_xxx_ball_clicked)
self.xxx_ball.position_changed.connect(self.on_xxx_ball_moved)
```

### E. 实现槽函数
```python
def on_xxx_ball_clicked(self):
    ball_pos = self.xxx_ball.geometry()
    self.xxx_panel.toggle_visibility(ball_pos.x(), ball_pos.y())

def on_xxx_ball_moved(self, x, y):
    if self.xxx_panel.isVisible():
        self.xxx_panel.update_position(x, y)
```

### F. 处理展开与恢复逻辑
1. 在 `toggle_sub_balls` 中增加对 `enable_xxx_ball` 选项的判断。
2. 在 `_restore_balls_after_screenshot` 中增加对应的 `show()` 调用。
3. 在 `on_action_triggered` 中增加快捷键触发逻辑。

## 5. 打包与发布
1. 如果引入了新的第三方库，记得更新 `requirements.txt`。

