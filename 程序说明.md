# 桌面人偶 (Desktop Assistant) - 源码深度解析与项目说明

本项目是一个基于 Python 3 和 PySide6 (Qt) 框架构建的 Windows 桌面悬浮辅助工具（桌面人偶-简洁版）。通过极简的悬浮球交互方式，集成了日常工作中最常用的快捷功能，并支持深度自定义，极大提升桌面办公效率。

## 🌟 核心特性概览

1. **主悬浮球与多级互动**: 
   - 悬停于桌面最上层，支持任意拖拽。
   - 单击展开/收起功能子球，右键呼出托盘菜单。
   - 支持高分屏 (DPI) 适配，自动加载项目图标 (`app_icon.ico`)。

2. **多功能子球生态**:
   - 📋 **剪贴板管理**: 后台自动监听剪贴板，支持文本、图片。面板支持点击重写剪贴板、预览、删除及一键清空。
   - ✂️ **系统截图**: 一键安全调用 Windows 原生截图 (`Win+Shift+S`)。
   - 📝 **悬浮记事本**: 极速呼出独立记事本界面，支持快速暂存文字，文本内容自动持久化保存。
   - 🎯 **智能截图**: 基于纯 Python 的屏幕元素识别，自动识别窗口边界和控件边缘，支持鼠标拖拽进行框选。
   - 🎥 **屏幕录制**: 极简划区录屏，附带视觉录制边框与倒计时提示，直接保存为 MP4 格式。
   - 🔍 **快速搜索**: 呼出快捷搜索框，按下回车直达默认浏览器引擎搜索。

3. **无限扩展与动态布局 (Concentric Layout)**: 
   - **同心圆算法布局**: 支持添加任意数量的子球（包含系统自带与用户自定义）。当子球超出单圈显示上限时，会自动扩展为多层同心圆排列，保证界面视觉不拥挤。
   - **自定义应用启动 (Custom Apps)**: 用户可进入设置面板添加外部 `.exe` 程序或快捷方式，工具将自动提取应用程序图标并生成一个专属的快捷启动子球。

4. **高级设置与全局热键**:
   - ⚙️ **设置面板**: 可视化设置面板 (`ui/settings.py`)，支持功能球显隐切换、文件保存路径自定义（截图、录屏）。
   - ⌨️ **全局快捷键**: 所有核心功能（展开球、截图、智能截图、录屏、搜索、记事本）均可配置全局组合快捷键唤醒。

## 📂 目录结构与模块解析

### 1. 核心启动枢纽 (`main.py`)
整个工具的入口、生命周期管理与事件总线。
- **UI & 核心桥接**: 初始化 `FloatingAssistant`，负责实例化所有 `ui` 与 `core` 组件。
- **同心圆布局调度**: 实现 `_update_balls_layout()` 算法，智能计算各子球的轨道与摆放角度。
- **单例限制**: 使用 `win32event.CreateMutex` 防止多开。

### 2. 界面视图层 (`ui/`)
负责与用户的一切视觉和操作交互。
- **`floating_ball.py` & `sub_ball.py`**: 定义了悬浮球体的基类及特殊形态。`SubBall` 现支持渲染文本 emoji 或提取的系统图标。
- **`panel.py`**: 剪贴板历史记录面板。
- **`notebook.py`**: 悬浮记事本界面组件。
- **`screenshot_mask.py` & `recording_border.py`**: 提供智能截图时的暗色遮罩与框选逻辑，以及录屏时的红色虚线边框与倒计时动画。
- **`search_box.py`**: 呼出式圆角搜索框。
- **`settings.py`**: 基于 QTabWidget 实现的综合设置窗体，囊括了基础设置、快捷键、自定义应用管理等标签页。

### 3. 核心业务层 (`core/`)
负责脱离界面的底层逻辑、系统 API 调用及数据管理。
- **`clipboard.py`**: 剪贴板历史管理器。利用定时器/监听器捕捉内容，支持多媒体文件的分离存储，维护 `history.json` 序列化。
- **`screenshot.py`**: 基于 `win32gui` 的窗口/控件边界自动化提取算法，供智能截图使用。
- **`screen_recorder.py`**: 利用多线程封装的高效屏幕录制引擎。
- **`hotkey.py`**: 基于 `keyboard` 模块进行全局快捷键监听及回调分发。
- **`notebook.py`**: 记事本数据本地持久化读写。

### 4. 辅助与工具层 (`system/` & `utils/`)
- **`system/tray.py`**: 创建 Windows 任务栏托盘图标及右键级联菜单，提供快捷配置项（如：隐藏/显示剪贴板、打开设置等）。
- **`utils/config.py`**: JSON 配置文件 (`config.json`) 读写与默认配置自动修复逻辑。
- **`utils/path_helper.py`**: 运行时绝对路径与相对路径的安全获取。

## 🚀 开发与打包说明

1. **环境依赖**:
   执行程序及开发需依赖 `PySide6`、`keyboard`、`Pillow` 等库（详见 `requirements.txt`）。对于底层窗口捕获依赖 `pywin32`。

2. **本地运行**:
   ```bash
   python main.py
   ```

3. **打包发布 (`桌面人偶.spec`)**:
   本项目已配置 PyInstaller 的 spec 文件，包含对隐藏依赖及各类资源文件的映射。可通过 `pyinstaller 桌面人偶.spec` 实现一键编译免安装的独立运行版本。
