# 桌面人偶 (Desktop Assistant) - 源码深度解析与项目说明

本项目是一个基于 Python 3 和 PySide6 (Qt) 框架构建的 Windows 桌面悬浮辅助工具（桌面人偶-简洁版）。
它的初衷是解决日常办公中“频繁切换软件、找文件、找截图工具”的痛点。通过一个极简的悬浮球，将**剪贴板增强、智能截图、极简录屏、近期文件追踪、快速启动**等刚需功能高度聚合。我们希望它成为你电脑桌面上最轻量、最全能的“瑞士军刀”。

---

## 🌟 核心特性概览（它能做什么？）

### 1. 主悬浮球与多级互动 (Main Ball)
- **无感悬浮与自由拖拽**: 悬浮球始终悬浮在桌面最上层（甚至在全屏看网页时也能看到）。按住鼠标左键即可把它拖到屏幕任意不碍事的角落。
- **一键展开/收起**: 单击主球，周围会像“花瓣”一样展开所有功能子球。再点一次即可全部收拢。
- **智能防遮挡 (截图自动隐藏)**: 考虑到大家截图时不想把“悬浮球”也截进去，程序内置了智能检测功能，在启动截图瞬间会自动隐身。

### 2. 七大功能子球生态 (Sub-ball Ecosystem)
程序原生自带 7 个颜色的功能子球，覆盖了绝大多数的高频操作：
- 📋 **剪贴板管理 (橙色)**: 强化版 Windows 剪贴板！不仅能存历史文字，还能存图片！我们加入了**超强深度去重算法**，彻底解决了复制时出现重复记录的 Bug。此外，支持左键带格式复制、右键纯文本复制，遇到网址还可以 `Ctrl+左键` 一键调用浏览器直达！
- ✂️ **系统截图 (绿色)**: 一键调用 Windows 10/11 自带的高级截图工具 (`Win+Shift+S`)。
- 📝 **悬浮记事本 (蓝色)**: 闪电般呼出一个无边框的便签本，随手记录灵感，**输入内容实时自动保存**，永不丢失。
- 🎯 **智能截图 (红色靶心)**: 纯 Python 自研的屏幕捕捉引擎。它能**像磁铁一样自动吸附**到你想要截取的窗口或按钮边缘。经过最新重构，它已经能完美应对多层窗口叠加、DPI 缩放错位等复杂系统环境，实现 100% 精准框选。
- 🎥 **屏幕录制 (红色摄像机)**: 抛弃笨重的录屏软件！只需一划，选中你要录的区域，支持 H.264 硬件加速的 MP4 录制，即录即存，不卡顿。
- 🔍 **快速搜索 (紫色)**: 呼出一个搜索框，输入任何问题按回车，瞬间打开浏览器搜索。
- 🕘 **最近使用 (青色)**: **[重磅新功能]** 自动追踪并列出你刚刚用过的所有文档、图片或项目代码。支持黑白名单过滤，再也不起到处翻文件夹找“刚才存的文件”了！

### 3. 同心圆算法与无限扩展 (Concentric Layout)
- **同心圆排版**: 如果你觉得 7 个功能不够，还可以无限制地添加更多功能。程序自带“智能同心圆算法”，子球数量超出单圈时，会自动扩展出第二圈、第三圈，确保无论加多少个球，都不会互相重叠。
- **自定义应用 (Custom Apps)**: 想要快速打开微信或游戏？只需在设置里把它们的 `.exe` 文件路径加进去，程序会自动为你生成一个带该软件图标的新子球！

### 4. 全局快捷键与深度配置 (Settings & Hotkeys)
- **设置中心**: 右键点击悬浮球可进入可视化设置页面。你可以自由决定每个子球的去留、修改视频保存路径、设置最大存储容量等。
- **全局快捷键**: 觉得用鼠标点太慢？你可以为所有功能绑定全局快捷键（比如 `Alt+Q` 录屏），在任何软件界面下按下快捷键就能直接唤醒对应功能。

---

## 📂 源码架构解析（代码是怎么组织的？）

本项目采用了非常规范的 **MVC / 模块化** 设计。如果你懂一点 Python，会发现它的逻辑极其清晰，非常适合二次开发。

### 1. 核心启动枢纽 (`main.py`)
这是整个程序的“心脏”。
- 负责启动 PySide6 的 `QApplication`。
- 把所有的 UI 界面（视图）和 Core 业务（逻辑）组合在一起。
- 管理悬浮球的显隐、计算同心圆的坐标，并将用户的点击操作分配给对应的后台功能。

### 2. 用户界面视图层 (`ui/` 文件夹)
这里存放着你能“看到”的所有东西。它们通常是不带业务逻辑的“纯界面”。
- **`floating_ball.py` & `sub_ball.py`**: 悬浮球体本身的绘制（渐变色、圆形裁切）和鼠标拖拽逻辑。
- **`panel.py` & `recent_panel.py`**: 剪贴板和最近使用记录的展示面板，处理列表的渲染、文字超长省略 (`...`)、图片的居中显示等。
- **`screenshot_mask.py`**: 智能截图时的黑色半透明遮罩层，以及绘制高亮橙色边框的逻辑。
- **`settings.py`**: 基于多标签页 (`QTabWidget`) 实现的现代化设置面板。

### 3. 底层核心业务层 (`core/` 文件夹)
这里是程序运转的“大脑”，负责与 Windows 系统底层打交道。
- **`clipboard.py`**: 使用系统钩子监听剪贴板，剥离特殊字符以实现精准去重，分离图片与文本流。
- **`screenshot.py`**: 调用 `win32gui` 和 `DWM API`，遍历当前屏幕上的所有真实窗口，提取出精确的坐标尺寸（Rect），提供给界面层去绘制吸附边框。
- **`screen_recorder.py`**: 负责将屏幕画面一帧一帧抓取下来，调用 OpenCV (`cv2`) 编码成视频文件。
- **`recent.py`**: 读取 Windows 系统内置的 `Recent` 文件夹，解析出用户最近打开的文件轨迹。
- **`hotkey.py`**: 使用 `keyboard` 模块在极低层级拦截键盘按键，实现全局热键。

### 4. 辅助与工具层 (`utils/` & `system/`)
- **`utils/config.py`**: 负责读写 `config.json`，把你的个性化设置持久化保存在电脑里。
- **`system/tray.py`**: 掌管系统右下角任务栏的那个小图标以及右键菜单。

---

## 🛠️ 如何编译与打包？

如果你修改了源码，想要把它变成一个“可以发给朋友直接双击运行”的 `.exe` 程序，请按以下步骤操作：

1. **配置环境**:
   确保你安装了 `PySide6`、`keyboard`、`Pillow`、`opencv-python`、`pywin32` 等库（可参考 `requirements.txt`）。
2. **运行测试**:
   直接在项目根目录下运行 `python main.py`，确认你的修改没有引发报错。
3. **一键打包**:
   我们在根目录提供了一个 `桌面人偶.spec` 配置文件。打开终端（Terminal），输入：
   ```bash
   pyinstaller 桌面人偶.spec
   ```
   打包完成后，你会在 `dist` 文件夹里看到一个已经封装好的独立程序文件夹，发给任何人都能直接运行！