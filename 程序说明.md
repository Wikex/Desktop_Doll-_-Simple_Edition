# 桌面人偶 - 程序说明与源码理解指南

本文档面向 AI 助手、开发者、维护者。目标是让读者快速理解这个项目是什么、如何运行、代码如何组织、各模块之间如何协作，以及修改功能时应该优先看哪些文件。

如果你是普通用户，只想知道怎么使用软件，请阅读 `详细使用手册.md`。

---

## 1. 项目定位

桌面人偶是一个 Windows 桌面悬浮工具，使用 Python 3 和 PySide6 构建。它通过一个常驻桌面的悬浮球，把剪贴板历史、截图、智能截图、录屏、记事本、最近使用文件、自定义软件启动等功能集中到一起。

程序强依赖 Windows 桌面能力，不是跨平台应用。核心依赖包括 PySide6、pywin32、keyboard、Pillow、uiautomation、mss、opencv-python、numpy、psutil。

主要能力：

- 主悬浮球：常驻桌面，支持拖动、右键菜单、展开子球。
- 贴边隐藏：主悬浮球拖到屏幕左右边缘后会半隐藏，左键点击露出的边缘区域可恢复显示。
- 位置提示：托盘菜单可触发“显示它在哪”，用轻量定位环标出主悬浮球位置，后续可替换为 Live2D 定位动画。
- 剪贴板历史：记录文字和图片，支持去重、带格式复制、纯文本复制、图片缓存。
- 智能截图：使用 Windows 窗口枚举、DWM 边界和 UIAutomation 进行窗口/控件吸附。
- 进阶截图：复用智能选区，在截取位置显示浮动编辑层，支持标注、复制、保存、定图和 OCR 入口。
- 屏幕录制：用户框选区域后，用 mss 抓屏、OpenCV 写入 mp4 或 webm。
- 悬浮记事本：输入内容自动保存到 `notebook.json`。
- 最近使用：扫描 Windows Recent 目录，列出近期打开文件，支持打开、定位、拖拽、过滤、置顶。
- 自定义应用：在设置里添加 `.exe` 后，生成新的子球用于快速启动。

---

## 2. 启动方式

源码运行：

```bash
python main.py
```

打包：

```bash
pyinstaller 桌面人偶.spec
```

打包配置文件是 `桌面人偶.spec`。它包含必要资源和动态导入提示：

- `datas`：`app_icon.ico`、`qt.conf`、`config.json`、`skins/`
- `hiddenimports`：`win32com.client`、`pythoncom`、`pywintypes`、`cv2`、`numpy`、`mss`、`uiautomation`、`comtypes`、`PIL.Image`、`PIL.ImageGrab`、`psutil`

---

## 3. 程序入口和生命周期

### `main.py`

`main.py` 是轻入口，只做两件事：

1. 通过 Windows Mutex 保证单实例运行。
2. 创建 `FloatingAssistant` 并进入 Qt 主循环。

如果程序已经运行，再启动一次会弹出提示：“桌面人偶已经在运行中。”

### `app_controller.py`

`app_controller.py` 是主控制器，核心类是 `FloatingAssistant`。它负责：

- 创建 `QApplication`
- 设置 DPI 感知和 Qt 插件路径
- 初始化主球、子球、托盘、各面板和核心管理器
- 连接 UI 信号和业务逻辑
- 处理截图、录屏、最近使用、剪贴板等用户动作
- 应用设置变更

后续如果要理解程序行为，优先看 `app_controller.py`。

---

## 4. 目录结构

```text
.
├── main.py                    # 轻入口：单实例保护 + 启动控制器
├── app_controller.py          # 主控制器：装配 UI、Core、信号、动作
├── actions/                   # 可复用动作辅助函数
├── core/                      # 核心业务和 Windows 底层逻辑
├── ui/                        # PySide6 界面组件
├── system/                    # 系统托盘
├── utils/                     # 配置、路径、日志等工具
├── skins/                     # 默认皮肤和后续皮肤配置
├── tools/diagnostics/         # 调试/诊断脚本归档
├── images/                    # 示例或资源图片
├── picture/                   # 剪贴板图片缓存目录，运行时生成/使用
├── video/                     # 录屏保存目录，运行时生成/使用
├── config.json                # 用户配置，运行时读写
├── history.json               # 剪贴板历史，运行时读写
├── recent_history.json        # 最近使用历史，运行时读写
├── notebook.json              # 记事本内容，运行时读写
└── 桌面人偶.spec              # PyInstaller 打包配置
```

运行数据文件通常不应该提交到 Git。当前 `.gitignore` 已忽略配置、历史、缓存、视频、图片、构建产物和本地编辑器配置。

---

## 5. 核心模块说明

### 5.1 `core/clipboard.py`

类：`ClipboardManager`

职责：

- 监听系统剪贴板变化。
- 记录文字和图片。
- 对文字做归一化去重，减少重复记录。
- 过滤 Office/WPS 格式刷等无意义剪贴板内容。
- 忽略 Windows Explorer 文件复制产生的 URL payload，避免文件路径污染剪贴板历史。
- 图片保存到 `picture/` 或配置中的图片缓存目录。
- 清理图片缓存时，只删除“不被剪贴板历史引用”的旧图片，避免面板里图片失效。
- 点击历史项时写回剪贴板，并自动发送 `Ctrl+V`。
- 支持记录置顶。历史项包含 `pinned` 和 `created_at` 元数据：
  - 新记录插入到所有置顶项下方、普通记录区最顶部。
  - 取消置顶后回到普通区，并按原始 `created_at` 重新排序。
  - 最大数量裁剪只裁剪普通记录，置顶记录不会被自动清理。

关键文件：

- 历史数据：`history.json`
- 图片缓存：`picture/` 或 `config.json` 中的 `picture_save_path`

注意：

- `psutil` 用于识别剪贴板 owner 进程，已写入 `requirements.txt`。
- 剪贴板相关异常会写到 `desktop_doll.log`。

### 5.2 `core/hotkey.py`

类：`HotkeyManager`

职责：

- 使用 Windows `RegisterHotKey` 注册全局快捷键。
- 监听 `WM_HOTKEY` 并发出 `action_triggered` 信号。
- 支持修改快捷键。
- 如果新快捷键注册失败，会恢复旧快捷键，避免用户原来的快捷键丢失。
- 每 5 分钟会刷新一次原生热键注册，降低长时间运行后 Windows 热键注册丢失的影响。
- 设置页编辑快捷键时会短暂停止响应热键，并带 120 秒超时恢复，避免焦点事件异常导致热键长期失效。
- 注册失败会记录到日志，并在设置页显示“快捷键被占用”类提示。

默认快捷键来自 `utils/config.py` 的 `DEFAULT_HOTKEYS`，用户配置保存在 `config.json`。

### 5.3 `core/screenshot.py`

职责：

- 使用 `ctypes` 直接调用 Windows API 枚举窗口。
- 使用 DWM `DwmGetWindowAttribute` 获取去除阴影后的真实窗口边界。
- 过滤桌面、任务栏、不可见窗口、cloaked 窗口、小窗口。
- 按面积从小到大排序候选矩形，让智能截图优先命中更具体的子窗口/控件。
- 使用虚拟屏幕范围处理多显示器和负坐标屏幕。
- 提供 UIAutomation 点位控件链查询，用于没有传统 HWND 的控件。

主要函数：

- `get_accurate_window_rect(hwnd)`
- `is_window_valid(hwnd)`
- `get_all_visible_rects()`
- `get_virtual_screen_rect()`
- `get_uia_rects_at(x, y)`
- `get_uia_rect_at(x, y)`

### 5.4 `core/screen_recorder.py`

类：`ScreenRecorderThread`

职责：

- 在后台线程里录屏，避免卡住 UI。
- 使用 `mss` 抓取屏幕区域。
- 使用 `cv2.VideoWriter` 写入视频。
- 支持 `mp4` 和 `webm`。
- 对录屏区域做最小尺寸校验，小于 `10 x 10` 会报错。
- 停止录屏后会释放 `VideoWriter`，再发出保存完成信号，避免文件句柄未释放。

录屏保存路径来自 `config.json` 的 `video_save_path`，默认是项目根目录下的 `video/`。

### 5.5 `core/recent.py` 和 `core/windows_recent.py`

类：`RecentManager`

职责：

- 扫描 Windows Recent 目录中的 `.lnk` 文件。
- 解析快捷方式目标路径。
- 忽略目录，只记录真实文件。
- 支持扩展名黑名单。
- 支持最大数量限制。
- 后台轮询周期为 15 秒。
- 用户打开最近使用面板时，会主动触发一次扫描，提高即时性。
- 支持文件置顶。置顶文件始终位于列表顶部；新文件记录会插入到最后一个置顶文件下面；取消置顶后按原始生成时间回到普通记录区；容量满额时只淘汰普通文件记录。

数据文件：

- `recent_history.json`

### 5.6 `core/notebook.py`

类：`NotebookManager`

职责：

- 读写 `notebook.json`
- 使用 800ms 防抖保存，避免每个按键都立刻写磁盘。

### 5.7 `core/skin_manager.py`

轻量皮肤配置管理。当前简单版不做复杂动效，但主流程已经读取默认皮肤配置，用它统一悬浮球、子球、面板和截图调试开关。

- 确保存在默认皮肤配置。
- 可以读取 `skins/<name>/skin.json`，并与 `DEFAULT_SKIN_CONFIG` 做深度合并，缺失字段会回落到默认值。
- 默认配置包含 `performance`、`main_ball`、`sub_ball`、`layout`、`panel`、`settings` 等分组。
- `performance.screenshot_debug_overlay` 控制智能截图遮罩上的调试文本，默认关闭。
- `main_ball.edge_hide` 控制主悬浮球左右贴边半隐藏行为。
- `main_ball.locator` 控制“显示它在哪”的定位提示。当前 `renderer` 为 `qt_ripple`，预留 `live2d_asset` 给后续美化版替换为 Live2D 动画。
- `sub_ball.colors` 控制内置子球和自定义软件子球颜色。
- `layout` 控制子球首圈半径、圈间距、拖拽最大半径。

后续美化版可以在这个接口上扩展精致皮肤、动态交互或 Live2D，但简单版应继续保持低资源占用，不应把重型动画逻辑塞进当前默认 UI。

---

## 6. UI 模块说明

### 6.1 `ui/floating_ball.py`

主悬浮球组件。

职责：

- 绘制主球。
- 支持拖动。
- 支持左右贴边半隐藏，左键点击露出的边缘区域恢复显示。
- 支持 `show_locator_hint()` 定位提示，用独立透明 Overlay 绘制扩散环；这是后续 Live2D 定位动画的替换点。
- 发出左键点击、右键点击、位置变化信号。
- 从皮肤配置读取尺寸、颜色和边框颜色。

### 6.2 `ui/sub_ball.py`

功能子球组件。

职责：

- 绘制子球。
- 根据主球位置和自身角度/半径计算坐标。
- 支持文字或图标。
- 支持拖动后保持相对关系。
- 从皮肤配置读取尺寸、字体、图标大小、边框颜色和拖拽范围。

子球布局由 `app_controller.py` 中的 `_update_balls_layout()` 管理。当前采用同心圆布局，子球数量多时自动扩展到外圈。首圈半径、球间距和圈间距由皮肤配置中的 `layout` 控制。

### 6.3 `ui/panel.py`

剪贴板历史面板。

职责：

- 展示文字和图片历史。
- 支持点击复制。
- 支持右键纯文本复制或定位图片文件。
- 支持 `Ctrl+左键` 打开剪贴板文本里的网址。
- 支持删除、清空、查看详情、拖拽导出。
- 默认按紧凑面板设计，尺寸和外边距可从皮肤配置读取。

### 6.4 `ui/recent_panel.py`

最近使用面板。

职责：

- 展示最近文件。
- 支持打开、定位、删除、清空。
- 支持拖拽文件到外部应用。
- 支持图片缩略图预览。
- 支持扩展名过滤和显示控制。
- 默认按紧凑面板设计，尺寸和外边距可从皮肤配置读取。

### 6.5 `ui/screenshot_mask.py`

截图/录屏框选遮罩。

职责：

- 覆盖所有屏幕。
- 支持智能吸附窗口/控件。
- 支持手动拖拽框选。
- 截图模式下把选区写入剪贴板。
- 进阶截图模式下把选区转为 `QPixmap`，同时传出逻辑坐标，交给 `ScreenshotEditor` 原地显示。
- 录屏模式下发出选区矩形给录屏逻辑。
- 智能吸附会组合 HWND 枚举候选、UIAutomation 候选和缓存候选，去重后按面积选择最具体的可用矩形。
- 左键按下和释放时都会补一次精确查询，减少快速点击时吸附还没更新的问题。
- 调试文本由 `show_debug_overlay` 控制，默认不显示，避免普通用户截图时看到开发信息。

### 6.6 `ui/screenshot_editor.py`

进阶截图编辑器。

职责：

- 在截取位置显示无边框浮动截图和工具栏。
- 高 DPI 下如果截图物理像素大于逻辑选区，会缩小到选区尺寸显示，不会放大截图。
- 支持画笔、矩形、箭头、文字、马赛克标注。画笔支持 `Shift` 直线和右键大小下拉菜单；文字标注有编辑态和完成态，编辑态显示轻边框并按内容自适应尺寸，完成态只绘制纯文字，支持拖动移动和双击重新编辑。
- 支持撤销、重做、清除标注，`Ctrl+Z` 撤销，`Ctrl+Y` 重做。
- 支持复制到剪贴板、快速保存、另存为、定图到桌面。
- 支持拖动工具栏移动整张浮动截图，避免遮挡目标内容。
- 提供 OCR 按钮，调用 `core/ocr.py`。当前 OCR 后端为可选 `pytesseract`，未安装时给出提示。

### 6.7 `ui/recording_border.py`

录屏区域边框和倒计时。

职责：

- 在录制区域显示红色边框。
- 开始录制前显示倒计时。

### 6.7 `ui/notebook.py`

悬浮记事本面板。

职责：

- 显示文本编辑区域。
- 内容变化后通知 `NotebookManager` 保存。

### 6.8 `ui/settings.py`

设置窗口。

主要标签页：

- 常规：截图时隐藏悬浮球、打开网址使用的浏览器程序。
- 剪贴板：监听开关、最大条数、图片缓存目录、图片缓存最大张数。
- 视频：录屏保存目录、保存格式。
- 最近使用：监听开关、最大条数、禁止记录管理。
- 快捷键：修改全局快捷键，显示快捷键冲突。
- 功能扩展：控制内置功能子球显示，添加/删除自定义软件。

设置保存逻辑：

- `_auto_save_options()` 会一次性写入 `config.json`，避免每个 key 单独写一次。
- 快捷键单独通过 `save_hotkeys()` 保存。

---

## 7. 动作辅助模块

`actions/` 用于把部分动作逻辑从主控制器中拆出来。

### `actions/clipboard_actions.py`

- `first_url(text)`：从文本中提取第一条 URL。
- `open_image_location(path)`：在资源管理器中定位图片文件。

### `actions/record_actions.py`

- `MIN_RECORD_SIZE = 10`
- `is_record_rect_valid(rect)`：判断录屏选区是否有效。
- `record_rect_error_message()`：返回用户提示文本。

---

## 8. 系统托盘

文件：`system/tray.py`

职责：

- 创建系统托盘图标。
- 提供右键菜单。
- 发出控制信号给 `FloatingAssistant`。

托盘菜单包含：

- 显示/隐藏悬浮球
- 显示它在哪
- 显示/隐藏所有面板
- 监听剪贴板变化
- 剪贴板历史容量
- 监听最近使用变化
- 最近使用最大条数
- 截屏时自动隐藏
- 设置
- 关于
- 退出

---

## 9. 配置和运行数据

### `config.json`

结构：

```json
{
  "hotkeys": {},
  "options": {}
}
```

默认值定义在 `utils/config.py`：

- `DEFAULT_HOTKEYS`
- `DEFAULT_OPTIONS`

新增全局动作时需要同步更新 `HotkeyKey`、`DEFAULT_HOTKEYS`、`ui/settings.py` 的快捷键行，以及 `app_controller.py::on_action_triggered()` 的分发逻辑。当前 `locate_ball` 默认绑定 `Ctrl+Shift+L`，用于触发“显示它在哪”定位提示。

`utils/config.py` 还定义了 `HotkeyKey`、`OptionKey`、`AppConfig`，用于给配置 key 提供类型约束，减少维护时拼错 key 的风险。

### 运行数据文件

- `history.json`：剪贴板历史。
- `recent_history.json`：最近使用历史。
- `notebook.json`：记事本内容。
- `desktop_doll.log`：轻量运行日志，记录关键异常。

这些文件是用户数据，不建议提交到 Git。

---

## 10. 关键用户流程

### 10.1 展开功能子球

1. 用户左键点击主球。
2. `FloatingBall.clicked` 触发。
3. `FloatingAssistant.toggle_sub_balls()` 判断当前是否有子球可见。
4. 如果未展开，按配置显示启用的子球和自定义应用子球。
5. 如果已展开，隐藏全部子球。

### 10.2 剪贴板记录

1. 系统剪贴板变化。
2. `ClipboardManager._on_clipboard_changed()` 启动防抖定时器。
3. `_process_clipboard()` 判断内容类型。
4. 如果是格式刷、文件复制等无效 payload，则忽略。
5. 如果是图片，保存为 PNG 文件并加入历史。
6. 如果是文字，归一化去重后加入历史。
7. 发出 `history_changed`，剪贴板面板刷新。

### 10.3 智能截图

1. 用户点击智能截图子球或按快捷键。
2. `FloatingAssistant.on_smart_screenshot_clicked()` 预抓屏幕背景和窗口矩形。
3. 打开 `ScreenshotMask`。
4. 鼠标移动时查找最佳吸附矩形。
5. 左键点击吸附区域，或拖拽手动框选。
6. 截图写入系统剪贴板。

### 10.4 进阶截图

1. 用户点击进阶截图子球或按快捷键。
2. `FloatingAssistant.on_advanced_screenshot_clicked()` 打开 `ScreenshotMask(mode="edit")`。
3. 用户吸附或手动框选区域。
4. `ScreenshotMask` 把选区截图和逻辑选区矩形发给 `ScreenshotEditor`。
5. 编辑器在截取位置附近显示浮动截图，基于原图和标注对象列表渲染结果。
6. 用户可复制、快速保存、另存为、定图或触发 OCR。

### 10.5 录屏

1. 用户点击录屏子球。
2. 打开 `ScreenshotMask(mode="record")`。
3. 用户拖拽选择区域。
4. 区域小于 `10 x 10` 会提示并取消。
5. 显示录屏边框和倒计时。
6. 倒计时结束后启动 `ScreenRecorderThread`。
7. 用户再次点击录屏子球停止录制。
8. 线程释放视频写入器后发出保存完成信号。
9. 弹出保存路径提示。

### 10.5 最近使用

1. 程序启动后 `RecentManager` 每 15 秒扫描一次。
2. 用户打开最近使用面板时，立即触发一次扫描。
3. 扫描 Windows Recent `.lnk`。
4. 解析目标路径。
5. 过滤目录、缺失文件、黑名单扩展名。
6. 更新 `recent_history.json` 并刷新面板。

---

## 11. 日志和排错

日志文件：`desktop_doll.log`

记录内容包括：

- 配置读写失败。
- 剪贴板历史读写失败。
- 图片缓存清理失败。
- 录屏异常。
- 最近使用扫描异常。
- 快捷键注册失败。
- 截图失败。
- 皮肤加载失败。

如果用户反馈“没有反应”“打不开”“截图失败”“录屏失败”，优先检查：

1. `desktop_doll.log`
2. `config.json`
3. 是否已有程序实例在运行
4. 是否缺依赖
5. 是否被安全软件拦截全局热键、截图或录屏

---

## 12. 开发和修改建议

### 修改某个功能时的入口

- 主球/子球展开逻辑：`app_controller.py`
- 剪贴板记录和复制：`core/clipboard.py`、`ui/panel.py`
- 最近使用：`core/recent.py`、`core/windows_recent.py`、`ui/recent_panel.py`
- 智能截图：`core/screenshot.py`、`ui/screenshot_mask.py`
- 录屏：`core/screen_recorder.py`、`ui/recording_border.py`、`app_controller.py`
- 快捷键：`core/hotkey.py`、`ui/settings.py`
- 设置页：`ui/settings.py`、`utils/config.py`
- 托盘菜单：`system/tray.py`
- 打包：`桌面人偶.spec`

### 修改原则

- 先看 `app_controller.py` 中信号连接，再看对应 `core/` 和 `ui/`。
- 不要直接删除用户数据文件。
- 不要把 `history.json`、`recent_history.json`、`notebook.json`、`picture/`、`video/` 提交进仓库。
- 涉及 Windows API 时优先保留 try/except，并写日志。
- GUI 功能修改后至少做一次 `python -B main.py` 冒烟启动。

### 推荐检查命令

```bash
python -B -c "import pathlib, tokenize; [compile(tokenize.open(p).read(), str(p), 'exec') for p in pathlib.Path('.').rglob('*.py')]; print('SYNTAX_OK')"
```

```bash
python -B -c "import main, app_controller, core.clipboard, core.hotkey, core.recent, core.screen_recorder, ui.settings; print('IMPORT_OK')"
```

```bash
python -B main.py
```

---

## 13. 已知限制

- 仅面向 Windows 桌面环境。
- 全局热键可能被其他程序占用。
- `keyboard`、录屏、截图能力可能被安全软件或权限策略拦截。
- 最近使用依赖 Windows 的 Recent 文件机制。如果系统关闭了最近文档记录，程序会尝试开启，但仍可能受系统策略影响。
- 高 DPI、多显示器、缩放混合环境中，智能截图依赖逻辑坐标和物理坐标转换，相关修改需要谨慎测试。

---

## 14. 调试脚本

临时诊断脚本已经归档到：

```text
tools/diagnostics/
```

这些脚本用于排查剪贴板格式、EMF/WMF、GDI、热键、窗口枚举等问题，不属于正常启动路径。
