# 桌面悬浮助手 - 完整源码解析与项目说明

本项目是一个基于 Python 和 PySide6 (Qt) 框架构建的 Windows 桌面悬浮球工具。项目通过极简的悬浮界面，集成了剪贴板历史记录、快捷屏幕截图、悬浮记事本、智能截图、屏幕录制、快速搜索及**自定义应用快捷启动**等功能。

## 核心功能概览

1. **拖拽式主悬浮球**: 悬停于桌面，提供主入口，支持自定义图标 (`app_icon.ico`)。
2. **剪贴板历史管理**: 后台自动记录复制的内容，悬浮面板一键查阅并重新复制（自动粘贴上屏）。
3. **系统屏幕截图**: 安全无冲突调用系统原生的截屏功能 (`Win+Shift+S`)。
4. **智能屏幕截图**: 自动识别窗口、控件边界，支持鼠标拖拽自定义区域。
5. **屏幕录制**: 支持自定义区域录制，带倒计时及实时边框提示，保存为 MP4 格式。
6. **快速搜索**: 弹出式搜索框，支持网络搜索。
7. **自定义应用启动 (New!)**: 支持在设置中添加本地程序（.exe 等），自动提取图标并在悬浮球周围生成快捷入口。
8. **同心圆动态布局 (New!)**: 采用智能布局算法，当功能球较多时自动以多层同心圆排列，防止界面拥挤。
9. **全局快捷键**: 支持自定义快捷键触发上述所有操作。

## 目录结构与核心模块解析

### 1. 核心入口 (`main.py`)
整个程序的启动枢纽与事件调度中心。
- **动态布局引擎**: 实现 `_update_balls_layout` 算法，根据子球数量自动计算半径与角度，支持无限扩展。
- **组件集成**: 整合固定功能球与用户自定义的 App 快捷球。
- **系统适配**: 处理 DPI 适配、全局单实例运行及窗口图标加载。

### 2. 用户界面 (`ui/`)
- **`floating_ball.py` & `sub_ball.py`**: 实现悬浮球基础交互。`SubBall` 现在支持显示系统图标（用于自定义应用）。
- **`panel.py` & `notebook.py`**: 提供剪贴板和记事本的交互界面。
- **`screenshot_mask.py` & `recording_border.py`**: 处理截图选区与录屏边框。
- **`search_box.py`**: 简洁的搜索输入组件。
- **`settings.py`**: 经过升级，支持用户管理自定义应用列表（添加、删除、禁用）。

### 3. 核心业务逻辑 (`core/`)
- **`clipboard.py`**: 剪贴板监听逻辑。
- **`screenshot.py`**: 智能边界识别算法。
- **`screen_recorder.py`**: 高速录屏引擎。
- **`hotkey.py`**: 全局热键注入。

### 4. 系统集成与工具
- **`system/tray.py`**: 托盘菜单。
- **`utils/config.py`**: 处理复杂的配置持久化，包括自定义应用路径。
- **`app_icon.ico`**: 程序的品牌视觉标识。

## 核心设计理念总结
1. **无限扩展性**: 通过**同心圆布局算法**，解决了功能球增多导致的 UI 遮挡问题。
2. **深度集成**: 自定义应用功能不仅是简单的启动，还包括了**自动图标提取**，使其与原生功能球视觉统一。
3. **极度轻量**: 即使在多层子球展开的情况下，依然保持极低的 CPU 和内存占用。
