# 桌面人偶 - 详细使用手册

这份手册写给完全不懂编程的用户。你只需要按步骤操作，就能使用桌面人偶的所有功能。

桌面人偶是一个常驻桌面的小工具。它平时显示为一个小圆球，点开后会出现多个小圆球，每个小圆球代表一个功能。

---

## 1. 第一次启动

### 1.1 如果你拿到的是打包版

1. 打开软件文件夹。
2. 找到 `桌面人偶.exe`。
3. 双击 `桌面人偶.exe`。
4. 屏幕右下角会出现一个蓝色悬浮球。

如果双击后没有反应，请看本手册最后的“常见问题”。

### 1.2 如果你拿到的是源码版

源码版需要电脑已经安装 Python 和依赖库。

1. 打开项目文件夹。
2. 在文件夹空白处按住 `Shift`，再点鼠标右键。
3. 选择“在终端中打开”或“在 PowerShell 中打开”。
4. 输入：

```bash
python main.py
```

5. 按回车。

正常情况下，屏幕右下角会出现一个蓝色悬浮球。

---

## 2. 认识主悬浮球

启动后，你会看到一个蓝色小圆球。这个小圆球就是“主悬浮球”。

### 2.1 移动悬浮球

1. 把鼠标移动到蓝色小圆球上。
2. 按住鼠标左键不放。
3. 拖到你想放的位置。
4. 松开鼠标左键。

你可以把它放到屏幕边缘、角落或任何不挡视线的位置。

### 2.2 贴边隐藏悬浮球

如果你觉得悬浮球挡住屏幕内容，可以把它拖到屏幕左边缘或右边缘。

1. 按住主悬浮球。
2. 拖到屏幕最左边或最右边。
3. 松开鼠标。
4. 悬浮球会自动缩到屏幕边缘，只露出一小条。
5. 需要使用时，用鼠标左键点击露出来的那一小条，悬浮球会重新显示出来。

贴边隐藏时，如果功能小球已经展开，程序会自动把功能小球收回。

### 2.3 打开功能小球

1. 用鼠标左键点击主悬浮球一次。
2. 主球周围会展开多个彩色小球。
3. 每个小球都是一个功能。
4. 再点主悬浮球一次，小球会收回。

### 2.4 打开右键菜单

1. 用鼠标右键点击主悬浮球。
2. 会出现一个菜单。

菜单里常用项目：

- 显示/隐藏悬浮球
- 显示它在哪
- 显示/隐藏所有面板
- 监听剪贴板变化
- 监听最近使用变化
- 截屏时自动隐藏
- 设置
- 关于
- 退出桌面人偶

### 2.5 找回悬浮球位置

如果你一时找不到主悬浮球，可以用托盘菜单定位它。

1. 找到任务栏右下角托盘图标。
2. 点击或右键打开菜单。
3. 点击“显示它在哪”。
4. 主悬浮球会显示出来，并出现一圈短暂的定位提示。

如果主悬浮球正在贴边隐藏，这个操作会先把它从屏幕边缘露出来。

### 2.6 退出程序

方法一：

1. 右键点击主悬浮球。
2. 点击“退出桌面人偶”。

方法二：

1. 找到任务栏右下角托盘图标。
2. 点击或右键打开菜单。
3. 点击“退出桌面人偶”。

---

## 3. 七个内置功能小球

展开主球后，默认会看到这些功能：

- 剪贴板历史
- 系统截屏
- 记事本
- 智能截屏
- 进阶截图
- 录屏
- 最近使用

如果你在设置里关闭了某个功能，它的小球就不会显示。

---

## 4. 剪贴板历史

剪贴板历史用于保存你复制过的文字和图片。

### 4.1 什么是剪贴板

平时你按 `Ctrl+C` 复制文字或图片，然后按 `Ctrl+V` 粘贴。中间保存复制内容的地方，就叫剪贴板。

Windows 自带剪贴板通常只能保存当前内容。桌面人偶会帮你保存历史记录。

### 4.2 打开剪贴板历史

1. 点击主悬浮球，展开小球。
2. 点击剪贴板小球。
3. 会弹出剪贴板历史面板。

### 4.3 复制历史文字

1. 打开剪贴板历史面板。
2. 找到你想重新使用的文字。
3. 用鼠标左键点击这条记录。
4. 程序会把这条内容放回剪贴板。
5. 到 Word、微信、浏览器或其他地方按 `Ctrl+V` 粘贴。

### 4.4 复制为纯文本

有些网页或 Word 里复制出来的文字带有字体、颜色、大小等格式。

如果你只想要干净的文字：

1. 打开剪贴板历史面板。
2. 找到那条文字记录。
3. 用鼠标右键点击它。
4. 程序会把它按纯文本放回剪贴板。
5. 到目标位置按 `Ctrl+V`。

### 4.5 使用历史图片

如果你复制过图片，面板里也会出现图片记录。

左键点击图片记录：

- 图片会重新进入剪贴板。
- 你可以到微信、Word、PPT 等地方按 `Ctrl+V` 粘贴。

右键点击图片记录：

- 程序会打开图片缓存所在的文件夹，并定位到这张图片。

### 4.6 拖拽剪贴板内容

部分内容可以直接拖出去：

1. 打开剪贴板历史面板。
2. 按住某条文字或图片记录。
3. 不要松手，拖到微信、文件夹、Word 等地方。
4. 松开鼠标。

这适合快速发送图片、拖出文件、整理内容。

### 4.7 打开网址

如果剪贴板记录里有网址：

1. 按住键盘 `Ctrl`。
2. 用鼠标左键点击那条记录。
3. 程序会打开浏览器访问网址。

### 4.8 置顶重要记录

如果某条文字或图片经常要用，可以把它置顶。

1. 打开剪贴板历史面板。
2. 找到要固定的记录。
3. 点击这条记录左侧的图钉按钮。
4. 记录会移动到列表顶部，并带有浅橙色背景。

置顶后的规则：

- 新复制的文字或图片不会盖到置顶记录上面，而是出现在最后一个置顶记录的下面。
- 如果历史数量达到上限，程序只会自动清理普通记录，不会自动清理已置顶记录。
- 如果你手动点击删除或清空，置顶记录也会被删除，请操作前确认。

取消置顶：

1. 找到已经置顶的记录。
2. 再点一次图钉按钮。
3. 它会回到普通记录区，并按它原本产生的时间排回合适位置。

### 4.9 删除和清空记录

删除单条：

1. 鼠标移动到某条记录上。
2. 点击删除按钮。

清空全部：

1. 打开剪贴板历史面板。
2. 找到面板底部的清空按钮。
3. 点击清空。

请注意：清空后，历史记录不能从软件里恢复。

---

## 5. 系统截屏

系统截屏会调用 Windows 自带截图工具。

### 使用方法

1. 点击主悬浮球，展开小球。
2. 点击系统截屏小球。
3. 屏幕上方会出现 Windows 截图工具。
4. 选择矩形截图、窗口截图或全屏截图。
5. 截图完成后，图片会进入系统剪贴板。
6. 到微信、Word、PPT 等地方按 `Ctrl+V` 粘贴。

### 如果没有反应

可能是快捷键被系统或其他软件拦截。你可以尝试手动按：

```text
Win + Shift + S
```

如果手动也没有反应，说明 Windows 截图工具本身可能有问题。

---

## 6. 智能截屏

智能截屏是桌面人偶自己的截图功能。它可以自动吸附窗口、按钮或控件边缘。

### 6.1 打开智能截屏

1. 点击主悬浮球。
2. 点击智能截屏小球。
3. 屏幕会变暗。

### 6.2 自动吸附截图

1. 打开智能截屏后，不要按鼠标。
2. 移动鼠标到你想截图的窗口或区域上。
3. 你会看到橙色边框。
4. 橙色边框包住你想截的内容后，点击鼠标左键。
5. 截图会进入剪贴板。
6. 到微信、Word、PPT 等地方按 `Ctrl+V` 粘贴。

### 6.3 手动框选截图

如果自动吸附不是你想要的范围：

1. 打开智能截屏。
2. 按住鼠标左键。
3. 拖出一个矩形框。
4. 松开鼠标。
5. 这个矩形区域会被截图。

### 6.4 取消智能截屏

按键盘 `Esc`，或点击鼠标右键，可以取消截图。

### 6.5 进阶截图

进阶截图会先选择截图区域，然后打开编辑窗口，不会立刻把图片复制走。

打开方式：

1. 点击主悬浮球。
2. 点击进阶截图小球。
3. 屏幕会变暗。
4. 可以像智能截屏一样自动吸附，也可以按住鼠标左键手动框选。
5. 松开鼠标后，会打开“进阶截图”编辑窗口。

编辑窗口里可以做这些事：

- 画笔：直接在截图上画线。
- 矩形：框出重点区域。
- 箭头：指向重点位置。
- 文字：点击截图后输入标注文字。
- 马赛克：拖出一块区域打码。
- 撤销/重做：回退或恢复标注。
- 复制：把编辑后的截图复制到剪贴板。
- 快速保存：保存到图片目录。
- 另存为：自己选择保存位置和格式。
- 定图：把当前截图固定到桌面上。
- 识别文字：尝试 OCR，并把识别结果复制到剪贴板。

OCR 说明：

- 默认只预留入口，不会常驻占用资源。
- 如果没有安装 OCR 组件，点击时会提示不可用。
- 后续安装 Tesseract 和 `pytesseract` 后，可以使用这个按钮识别文字。

---

## 7. 录屏

录屏用于录制屏幕上的一块区域。

### 7.1 开始录屏

1. 点击主悬浮球。
2. 点击录屏小球。
3. 屏幕会变暗。
4. 按住鼠标左键拖出你要录制的区域。
5. 松开鼠标。
6. 屏幕上会出现录制区域边框。
7. 程序会显示倒计时。
8. 倒计时结束后开始录制。
9. 录制中，录屏小球会变成绿色。

### 7.2 停止录屏

1. 录制中，再次点击绿色录屏小球。
2. 程序会停止录制。
3. 视频文件保存完成后，会弹出提示，告诉你保存位置。

### 7.3 录屏文件保存在哪里

默认保存在项目文件夹下的：

```text
video/
```

也可以在设置里修改保存目录。

### 7.4 录屏格式

支持：

- `mp4`
- `webm`

默认是 `mp4`。

### 7.5 区域太小怎么办

如果你框选的区域小于 `10 x 10` 像素，程序会提示区域太小。

解决方法：

1. 重新点击录屏小球。
2. 框选一个更大的区域。

---

## 8. 悬浮记事本

记事本适合临时记录文字，比如电话号码、待办事项、复制来的资料。

### 打开记事本

1. 点击主悬浮球。
2. 点击记事本小球。
3. 会弹出一个记事本窗口。

### 输入和保存

直接输入文字即可。

你不需要点击保存按钮。程序会自动保存内容。

下次再打开，之前写的内容还在。

### 清空内容

如果记事本面板里有清空按钮，点击即可清空。

清空后请确认你已经不需要这些内容。

---

## 9. 最近使用

最近使用会列出你最近打开过的文件，比如 Word、Excel、PPT、图片、PDF、代码文件等。

### 9.1 打开最近使用

1. 点击主悬浮球。
2. 点击最近使用小球。
3. 会弹出最近使用面板。

打开面板时，程序会立即扫描一次最近文件，所以新文件通常能较快出现。

### 9.2 打开文件

1. 在最近使用面板中找到文件。
2. 双击它。
3. 文件会用系统默认程序打开。

### 9.3 打开文件所在位置

1. 找到文件。
2. 用鼠标右键点击它。
3. 程序会打开资源管理器，并定位到这个文件。

### 9.4 拖拽文件

1. 在最近使用面板中按住一个文件。
2. 拖到微信、QQ、文件夹、桌面或其他程序里。
3. 松开鼠标。

这适合快速发送刚刚用过的文件。

### 9.5 图片预览

如果最近使用里有图片：

1. 鼠标停在图片文件上。
2. 稍等一下。
3. 面板旁边会显示图片缩略图。

### 9.6 置顶常用文件

如果某个文件经常要打开或发送，可以把它置顶。

1. 打开最近使用面板。
2. 找到要固定的文件。
3. 点击文件左侧的图钉按钮。
4. 文件会移动到列表顶部，并带有浅橙色背景。

置顶后的规则：

- 新出现的最近文件会排在所有置顶文件下面。
- 最近使用达到最大条数时，程序只会自动清理普通文件记录，不会自动清理已置顶文件。
- 如果取消置顶，文件会回到普通区，并按它原本进入记录的时间重新排序。

### 9.7 不想记录某类文件

例如你不想记录 `.tmp` 或某类文件：

1. 打开最近使用面板。
2. 点击“禁止记录管理”。
3. 添加你不想记录的扩展名。
4. 保存后，这类文件不会继续出现在列表里。

---

## 10. 设置

打开设置：

1. 右键点击主悬浮球。
2. 点击“设置”。

设置窗口分为多个页面。

### 10.1 常规

可以设置：

- 截屏时自动隐藏悬浮球
- 浏览器程序

“截屏时自动隐藏悬浮球”开启后，截图或录屏框选时，主球和子球会自动隐藏，避免被截进去。

### 10.2 剪贴板

可以设置：

- 是否监听剪贴板变化
- 剪贴板历史最大条数
- 图片缓存目录
- 图片缓存最大张数

如果你觉得历史记录太多，可以调小最大条数。

如果你经常复制图片，可以把图片缓存目录改到空间更大的磁盘。

### 10.3 视频

可以设置：

- 录屏保存目录
- 录屏保存格式

建议普通用户使用 `mp4`。

### 10.4 最近使用

可以设置：

- 是否监听最近使用变化
- 最近使用最大条数
- 禁止记录管理

如果你不想让程序记录最近文件，可以关闭监听。

### 10.5 快捷键

这里可以修改全局快捷键。

修改方法：

1. 点击某个快捷键输入框。
2. 直接按你想设置的组合键。
3. 例如按住 `Ctrl` 和 `Shift`，再按 `F`。
4. 输入框会显示类似 `ctrl+shift+f`。

如果快捷键被其他软件占用，程序会提示冲突，并保留原来的快捷键。

### 10.6 功能扩展

这里可以控制哪些功能小球显示。

如果你不想看到某个小球：

1. 打开“功能扩展”。
2. 找到对应功能。
3. 取消勾选。

以后展开主球时，这个小球就不会出现。

---

## 11. 添加自己的软件快捷启动

你可以把常用软件加成一个新的小球，比如微信、QQ、浏览器、IDE、游戏等。

### 添加方法

1. 右键点击主悬浮球。
2. 点击“设置”。
3. 打开“功能扩展”。
4. 找到“自定义软件快捷启动”。
5. 点击“添加新功能”。
6. 输入软件名称。
7. 点击“浏览...”。
8. 选择软件的 `.exe` 文件。
9. 点击确定。
10. 回到主球，展开小球，就能看到新加的软件小球。

### 使用方法

点击这个自定义小球，程序会启动对应软件。

### 删除方法

1. 打开“设置”。
2. 打开“功能扩展”。
3. 找到自定义软件。
4. 点击旁边的删除按钮。

---

## 12. 全局快捷键

全局快捷键的意思是：即使你正在使用 Word、浏览器、微信，也可以按快捷键唤出桌面人偶功能。

默认快捷键以你的 `config.json` 为准。常见功能包括：

- 剪贴板历史
- 系统截屏
- 智能截屏
- 进阶截图，默认 `Ctrl + Shift + A`
- 记事本
- 显示/隐藏悬浮球
- 显示它在哪，默认 `Ctrl + Shift + L`
- 录屏
- 最近使用
- 显示/隐藏所有面板

如果某个快捷键没反应，可能原因：

- 快捷键被其他软件占用。
- 设置里没有绑定该快捷键。
- 安全软件拦截了全局热键。

解决方法：

1. 右键主悬浮球。
2. 打开设置。
3. 进入“快捷键”。
4. 改成不常用的组合键。

---

## 13. 文件保存位置

### 13.1 剪贴板历史

文字和图片记录信息保存在：

```text
history.json
```

图片文件默认保存在：

```text
picture/
```

### 13.2 最近使用历史

保存在：

```text
recent_history.json
```

### 13.3 记事本内容

保存在：

```text
notebook.json
```

### 13.4 录屏视频

默认保存在：

```text
video/
```

### 13.5 日志文件

如果程序出错，错误信息可能保存在：

```text
desktop_doll.log
```

如果你向别人求助，可以把这个日志文件发给对方看。

---

## 14. 常见问题

### 14.1 双击后提示“桌面人偶已经在运行中”

说明程序已经打开了。

你可以：

1. 看屏幕角落有没有悬浮球。
2. 看任务栏右下角托盘区有没有图标。
3. 如果找不到，打开任务管理器，结束已有的桌面人偶进程后再启动。

### 14.2 找不到悬浮球

可能原因：

- 被拖到屏幕边缘。
- 被隐藏。
- 多显示器环境下在另一个屏幕。

解决方法：

1. 看任务栏右下角托盘图标。
2. 打开托盘菜单。
3. 点击“显示/隐藏悬浮球”。

### 14.3 剪贴板没有记录

请检查：

1. 右键主悬浮球。
2. 看菜单里的“监听剪贴板变化”是否开启。
3. 打开设置。
4. 进入“剪贴板”。
5. 确认“监听剪贴板变化”已勾选。

如果复制的是文件本身，程序可能会故意忽略，避免文件路径污染历史记录。

### 14.4 图片复制后看不到

可能原因：

- 图片记录功能关闭。
- 图片缓存目录没有写入权限。
- 图片太特殊，程序无法读取。

解决方法：

1. 打开设置。
2. 进入“剪贴板”。
3. 检查图片缓存目录。
4. 换一个普通文件夹，例如桌面上的新文件夹。

### 14.5 截图时把悬浮球也截进去了

解决方法：

1. 右键主悬浮球。
2. 打开设置。
3. 进入“常规”。
4. 勾选“截屏时自动隐藏悬浮球”。

### 14.6 录屏失败

可能原因：

- 选区太小。
- 保存目录没有权限。
- OpenCV 视频编码器不可用。
- 被安全软件拦截。

解决方法：

1. 选一个更大的区域。
2. 打开设置，进入“视频”。
3. 把保存目录改到桌面或其他普通文件夹。
4. 把格式改成 `mp4`。
5. 再试一次。

### 14.7 快捷键没反应

可能原因：

- 快捷键被其他软件占用。
- 程序没有成功注册快捷键。
- 当前输入法或软件拦截了快捷键。

解决方法：

1. 打开设置。
2. 进入“快捷键”。
3. 换一个组合键。
4. 尽量使用不常见组合，例如 `Ctrl + Alt + F8`。

### 14.8 最近使用没有新文件

可能原因：

- Windows 最近文件功能关闭。
- 文件类型被你加入了禁止记录。
- 程序还没扫描到。

解决方法：

1. 打开最近使用面板。
2. 等几秒钟。
3. 检查“禁止记录管理”。
4. 确认对应扩展名没有被禁止。

### 14.9 网址没有用指定浏览器

检查：

1. 右键主悬浮球。
2. 打开设置。
3. 进入“常规”。
4. 检查“浏览器程序”是否选择了正确的 `.exe`。

如果路径无效，程序会回退到系统默认浏览器。

### 14.10 程序出错怎么排查

找到项目文件夹里的：

```text
desktop_doll.log
```

打开它，看最后几行错误信息。

---

## 15. 给新手的推荐用法

如果你第一次用，不需要马上设置所有东西。建议这样开始：

1. 先把主悬浮球拖到屏幕右下角。
2. 试用剪贴板历史。
3. 试用系统截屏。
4. 试用智能截屏。
5. 试用最近使用。
6. 如果你经常录教程，再设置录屏保存目录。
7. 如果你常用某个浏览器，再设置浏览器程序。
8. 如果你觉得小球太多，就在“功能扩展”里关闭不常用的小球。

只要程序开着，它就会在桌面角落等待你使用。
